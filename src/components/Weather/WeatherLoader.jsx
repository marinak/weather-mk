import React from "react"
import { Background, Loader } from "../common"

const WeatherLoader = () => {
    return (
        <div>
            <Loader />
        </div>
    )
}

export default WeatherLoader